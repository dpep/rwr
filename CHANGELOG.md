# Changelog

## Unreleased

## 0.0.1 - 2026-08-20

Namespace placeholder. No functionality — `rwr` parses arguments and reports that its verbs
are unimplemented. The design lives in `DESIGN.md` and `docs/`; Phase 0, which decides whether
the tool should exist at all, has not yet run.

Published early only to hold the crates.io name. `0.1.0` is deliberately left free for the
first real release.
