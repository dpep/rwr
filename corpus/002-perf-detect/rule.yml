# The ideal rule alternates select/find_all via a `where:` predicate, which is
# not built yet (see docs/rule-corpus.md). This is the expressible subset.
match: $R.select { |$P| $B }.first
rewrite: $R.detect { |$P| $B }
