description: An explicit `return nil` says nothing `return` does not.
match: return nil
rewrite: return
tests:
  - input: |
      def a
        return nil
      end
    output: |
      def a
        return
      end
